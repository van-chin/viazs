import { withInstall } from "@viaz/utils";
import CheckboxInputNumber from "./src/index.vue";

const VzCheckboxInputNumber = withInstall(CheckboxInputNumber);

export { VzCheckboxInputNumber };

export default VzCheckboxInputNumber;
